{
  "name": "asterim-pipeline",
  "version": "0.1.0",
  "description": "Local development pipeline runner coordinating orchestrator/coder/tester agents via repository files",
  "type": "module",
  "bin": {
    "asterim-pipeline": "bin/asterim-pipeline.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --import ./test/setup.mjs --test --test-timeout=120000 \"test/*.test.js\"",
    "lint": "eslint .",
    "typecheck": "tsc -p jsconfig.json"
  },
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "typescript": "^7.0.2"
  }
}
